文件清单：
1.reprot 1-5 是五个实验报告。
2.code文件夹中共9个目录，分别存放9个题目(第2题另分为5个片段)的源代码
	其中以ex开头的是解决本题的主程序，其余为函数。
3.result目录中，存放8个wav文件，除E外均有输出结果。
	考虑到原语音由老师发出，该文件夹中未包含原语音zip文件。
4.support目录，存放一个txt文本，内有参考代码的网址。

说明：

1.如有文件缺漏或无法执行请邮件联系bzp15@mails.tsinghua.edu.cn或者微信：15582894499.
2.提交的所有.m文件最后检查时为了方便用了xcode打开，应该不会影响运行，如果无法运行请将代码copy到新建的matlab工程中即可。
3.整理report时时间仓促，没有提前做好规划，导致一些在第一题中应该提到的问题没有说明，见report_5的说明部分。(copy如下)
——————————————————————————————————————————————————————————————————————————————————————————
report_5:
一些说明：
    1.关于第四题的语音处理程序，可以见ex4.m，修改文件路径即可执行。实际运用时，还需要根据语音片段的内容来修改’rate’的值，目前默认为24000，即3s的语音。
    2.提交的代码为调试完第二题后的代码，rate值与提交的result文件可能略有出入，应该不影响最后结果。
    3.在程序运行时，matlab命令行可能会有几个输出，系debug时去掉“；”或者测试中间变量漏修改的结果，不影响最终输出(但是影响美观)。
    4.对于本题的四个模型，ex1，ex2都会将原语音片段全部删除生成一个新的片段。ex3，ex4会在原目录下生成一个out目录存放中间片段，之后原计划删除，但是由于当时设置没有管理员权限所以没有处理，后期忘记了，回看时发现有误却没时间修改。目前输出不会有错，但是会多处一个out文件夹，内有处理时未用到的语音片段。可以等程序运行结束后手动删除。
